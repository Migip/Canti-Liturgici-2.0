export enum funcSortValues{
    bigger = 1,
    equal = 0,
    smaller = -1
}

export const enum BusyIndicator {
    none = 'n',
    charging = 'c',
    downloading = 'd',
    noInit = 'i', //impossibile inizializzare l'app
}